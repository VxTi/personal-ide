async function test()
{
    function_call();

    let x = 1 + 1; // Numbers

    let lambda = (x) => {};

    let lambdaResult = lambda(1);

    let string = 'Hello, World!'; // Strings

    // Comments

    /**
     * Multi-line
     */
}

class Test {
    constructor()
    {
        this.x = 1;
    }
}